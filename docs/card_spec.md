# Card specification

Generated by `tools/dump_card_spec.py` from the game's own Lua database.
Do not edit by hand.

`effect_spec` entries are the game's internal C# effect-function names, which
resolve ambiguities in the printed card text (regions, per-country caps, and
which side acts). Treat them as the specification.

## Early War

### #1 Asia Scoring  [IMPLEMENTED]

- side: **Neutral**, ops: **0**, stage: Early War
- flags: must be played, scoring: Asia
- text:

  > Both sides score:
  > Presence: 3
  > Domination: 7
  > Control: 9
  > +1 per controlled Battleground Country in Region
  > +1 per Country controlled that is adjacent to enemy superpower

- effect_spec:

  - **event_effects**:
    - `ScoreAsia`(0)

### #2 Europe Scoring  [IMPLEMENTED]

- side: **Neutral**, ops: **0**, stage: Early War
- flags: must be played, scoring: Europe
- text:

  > Both sides score:
  > Presence: 3
  > Domination: 7
  > Control: VICTORY
  > +1 per controlled Battleground Country in Region
  > +1 per Country controlled that is adjacent to enemy superpower

- effect_spec:

  - **event_effects**:
    - `ScoreEurope`(0)

### #3 Middle East Scoring  [IMPLEMENTED]

- side: **Neutral**, ops: **0**, stage: Early War
- flags: must be played, scoring: Middle East
- text:

  > Both sides score:
  > Presence: 3
  > Domination: 5
  > Control: 7
  > +1 per controlled Battleground Country in Region

- effect_spec:

  - **event_effects**:
    - `ScoreMiddleEast`(0)

### #4 Duck and Cover  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Early War
- text:

  > Degrade DEFCON one level.
  > Then US player earns VPs equal to 5 minus current DEFCON level.

- effect_spec:

  - **event_effects**:
    - `RestorePendingDefconLevel`(0)
    - `DegradeDEFCONLevel`(1)
    - `GainVictoryPointsForDEFCONBelow`(5)

### #5 Five-Year Plan  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Early War
- text:

  > USSR player must randomly discard one card. If the card is a US associated Event, the Event occurs immediately. If the card is a USSR associated Event or and Event applicable to both players, then the card must be discarded without triggering the Event.

- effect_spec:

  - **event_effects**:
    - `CommitPlayerDecision`(0)
    - `RestorePendingDefconLevel`(0)
    - `OpponentDiscardsRandomCard`(1)

### #6 The China Card  [IMPLEMENTED]

- side: **Neutral**, ops: **4**, stage: Early War
- flags: cannot headline
- text:

  > Begins the game with the USSR player.
  > +1 Operations value when all points are used in Asia. Pass to opponent after play.
  > +1 VP for the player holding this card at the end of Turn 10.
  > Cancels effect of 'Formosan Resolution' if this card is played by the US player.

- effect_spec:

  - **global_continuous_effects**:
    - **effect**:
      - `GainOperationsPointsWhenUsingThisCardInAsia`(1)

### #7 Socialist Governments  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Early War
- text:

  > Unplayable as an event if 'The Iron Lady' is in effect.
  > Remove US Influence in Western Europe by a total of 3 Influence points, removing no more than 2 per country.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `RemoveOpponentInfluenceInWesternEuropeMax2`(3)
      - `PauseForAnimationToMap`(0)
    - **usageconditions**:
      - `IfHasNotBeenPlayedIronLady`(0)
      - `IfCrisisResult1945UKElectionIsNot4or5`(0)

### #8 Fidel  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Remove all US Influence in Cuba. USSR gains sufficient Influence in Cuba for Control.

- effect_spec:

  - **event_effects**:
    - `RemoveAllOpponentInfluenceInCuba`(0)
    - `GainInfluenceForControlInCuba`(0)
    - `PauseForAnimationToMap`(0)

### #9 Vietnam Revolts  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Add 2 USSR Influence in Vietnam. For the remainder of the turn, the Soviet player may add 1 Operations point to any card that uses all points in Southeast Asia.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `GainOperationsPointsInSoutheastAsia`(1)
  - **event_effects**:
    - `GainInfluenceInVietnam`(2)
    - `PauseForAnimationToMap`(0)
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #10 Blockade  [IMPLEMENTED]

- side: **USSR**, ops: **1**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Unless US Player immediately discards a '3' or more value Operations card, eliminate all US Influence in West Germany.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `OpponentMayDiscardCardWithOpsPoints`(3)
      - **_seq**:
        - `RemoveAllOpponentInfluenceInWestGermany`(0)
      - **condition**:
        - `IfYouDoNot`(0)
      - `PauseForAnimationToMap`(0)
    - **usageconditions**:
      - `IfCrisisResultVEDayIsNot6`(0)

### #11 Korean War  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Early War
- flags: remove-on-event (*)
- text:

  > North Korea invades South Korea. Roll one die and subtract 1 for every US Controlled country adjacent to South Korea. USSR Victory on modified die roll 4-6. USSR add 2 to Military Ops Track.
  > Effects of Victory: USSR gains 2 VP and replaces all US Influence in South Korea with USSR Influence.

- effect_spec:

  - **event_effects**:
    - `CommitPlayerDecision`(0)
    - `WarInSouthKorea`(514)   (packed: low=2, high=2)

### #12 Romanian Abdication  [IMPLEMENTED]

- side: **USSR**, ops: **1**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Remove all US Influence in Romania. USSR gains sufficient Influence in Romania for Control.

- effect_spec:

  - **event_effects**:
    - `RemoveAllOpponentInfluenceInRomania`(0)
    - `GainInfluenceForControlInRomania`(0)
    - `PauseForAnimationToMap`(0)

### #13 Arab-Israeli War  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Early War
- text:

  > A Pan-Arab Coalition invades Israel. Roll one die and subtract 1 for US Control of Israel and for US-controlled country adjacent to Israel. USSR Victory on modified die roll 4-6. USSR adds 2 to Military Ops Track.
  > Effects of Victory: USSR gains 2 VP and replaces all US Influence in Israel with USSR Influence.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `CommitPlayerDecision`(0)
      - `WarInIsrael`(514)   (packed: low=2, high=2)
    - **usageconditions**:
      - `IfHasNotBeenPlayedCampDavidAccords`(0)

### #14 COMECON  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Add 1 USSR Influence in each of four non-US Controlled countries in Eastern Europe.

- effect_spec:

  - **event_effects**:
    - `AddInfluenceInUncontrolledEasternEuropeMax1`(4)

### #15 Nasser  [IMPLEMENTED]

- side: **USSR**, ops: **1**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Add 2 USSR Influence in Egypt. Remove half (rounded up) of the US Influence in Egypt.

- effect_spec:

  - **event_effects**:
    - `GainInfluenceInEgypt`(2)
    - `RemoveHalfOpponentInfluenceInEgpyt`(0)
    - `PauseForAnimationToMap`(0)

### #16 Warsaw Pact Formed  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Remove all US Influence from four countries in Eastern Europe, or add 5 USSR Influence in Eastern Europe, adding no more than 2 per country. Allow play of NATO.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - **_seq**:
        - `RemoveAllOpponentInfluenceFromEasternEurope`(4)
        - `PauseForAnimationToMap`(0)
      - **description**:
      - **_seq**:
        - `AddInfluenceInEasternEuropeMax2`(5)
      - **description**:
    - **prompt**:
    - **_seq**:
      - `PutThisCardInPlayOppOwner`(0)
    - **condition**:
      - `IfHasNotBeenPlayedNATO`(0)

### #17 De Gaulle Leads France  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Remove 2 US Influence in France, add 1 USSR Influence. Cancels effects of NATO for France.

- effect_spec:

  - **event_effects**:
    - `RemoveOpponentInfluenceInFrance`(2)
    - `GainInfluenceInFrance`(1)
    - `PauseForAnimationToMap`(0)
    - `PutThisCardInPlay`(0)

### #18 Captured Nazi Scientist  [IMPLEMENTED]

- side: **Neutral**, ops: **1**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Advance player's Space Race marker one box.

- effect_spec:

  - **event_effects**:
    - `AdvanceSpaceRaceTrack`(1)

### #19 Truman Doctrine  [IMPLEMENTED]

- side: **USA**, ops: **1**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Remove all USSR Influence markers in one uncontrolled country in Europe.

- effect_spec:

  - **event_effects**:
    - `RemoveAllOpponentInfluenceFromUncontrolledInEurope`(1)
    - `PauseForAnimationToMap`(0)

### #20 Olympic Games  [IMPLEMENTED]

- side: **Neutral**, ops: **2**, stage: Early War
- text:

  > Player sponsors Olympics. Opponent may participate or boycott. If Opponent participates, each player rolls one die, with the sponsor adding 2 to his roll. High roll gains 2 VP. Reroll ties If Opponent boycotts, degrade DEFCON one level and the Sponsor may Conduct Operations as if they played a 4 Ops card.

- effect_spec:

  - **event_effects**:
    - `ResolveOlympicGames`(4)

### #21 NATO  [IMPLEMENTED]

- side: **USA**, ops: **4**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Play after 'Marshall Plan' or 'Warsaw Pact'.
  > USSR player may no longer make Coup or Realignment rolls in any US Controlled countries in Europe. US Controlled countries in Europe may not be attacked by play of the Brush War event.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `OpponentCannotCoupOrRealignInControlledEurope`(0)
  - **event_effects**:
    - **_seq**:
      - `PutThisCardInPlay`(0)
      - `RemoveMarshallPlanFromPlay`(0)
      - `RemoveWarsawPactFormedFromPlay`(0)
    - **usageconditions**:
      - `IfHasBeenPlayedMarshallPlanOrWarsawPact`(0)

### #22 Independent Reds  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Adds sufficient US Influence in either Yugoslavia, Romania, Bulgaria, Hungary, or Czechoslavakia to equal USSR Influence.

- effect_spec:

  - **event_effects**:
    - `MatchOpponentInfluenceForIndependentReds`(1)

### #23 Marshall Plan  [IMPLEMENTED]

- side: **USA**, ops: **4**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Allows play of NATO.
  > Add one US Influence in each of seven non-USSR Controlled Western European countries.

- effect_spec:

  - **event_effects**:
    - `AddInfluenceInUncontrolledWesternEuropeMax1`(7)
    - **_seq**:
      - `PutThisCardInPlay`(0)
    - **condition**:
      - `IfHasNotBeenPlayedNATO`(0)

### #24 Indo-Pakistani War  [IMPLEMENTED]

- side: **Neutral**, ops: **2**, stage: Early War
- text:

  > India or Pakistan invades the other (player's choice). Roll one die and subtract 1 for every opponent-controlled country adjacent to the target of the invasion. Player Victory on modified die roll of 4-6. Player adds 2 to Military Ops Track. Effects of Victory: Player gains 2 VP and replaces all opponent's Influence in target country with his Influence.

- effect_spec:

  - **event_effects**:
    - `WarInIndiaOrPakistan`(514)   (packed: low=2, high=2)

### #25 Containment  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Early War
- flags: remove-on-event (*)
- text:

  > All further Operations cards played by US this turn add one to their value (to a maximum of 4).

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `IncreaseOperationsPointsForYourCards`(1)
  - **event_effects**:
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #26 CIA Created  [IMPLEMENTED]

- side: **USA**, ops: **1**, stage: Early War
- flags: remove-on-event (*)
- text:

  > USSR reveals hand this turn.
  > Then the US may Conduct Operations as if they played a 1 Op card.

- effect_spec:

  - **event_effects**:
    - `CommitPlayerDecision`(0)
    - `RestorePendingDefconLevel`(0)
    - `OpponentRevealsHand`(0)
    - `ConductOperationsWithThisCard`(1)

### #27 US/Japan Mutual Defense Pact  [IMPLEMENTED]

- side: **USA**, ops: **4**, stage: Early War
- flags: remove-on-event (*)
- text:

  > US gains sufficient Influence in Japan for Control. USSR may no longer make Coup or Realignment rolls in Japan.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `OpponentCannotCoupOrRealignInJapan`(0)
  - **event_effects**:
    - `GainInfluenceForControlInJapan`(0)
    - `PauseForAnimationToMap`(0)
    - `PutThisCardInPlay`(0)

### #28 Suez Crisis  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Remove a total of 4 US Influence from France, the United Kingdom or Israel. Remove no more than 2 Influence per country.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `RemoveOpponentInfluenceForSuezCrisis`(4)
      - `PauseForAnimationToMap`(0)
    - **usageconditions**:
      - `IfCrisisResult1945UKElectionIsNot6`(0)

### #29 East European Unrest  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Early War
- text:

  > In Early or Mid War: Remove 1 USSR Influence from three countries in Eastern Europe.
  > In Late War: Remove 2 USSR Influence from three countries in Eastern Europe.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `Remove1OpponentInfluenceFromEasternEuropeCountries`(3)
    - **condition**:
      - `IfTurnNumberLessThan`(7)
    - **_seq**:
      - `Remove2OpponentInfluenceFromEasternEuropeCountries`(3)
    - **condition**:
      - `IfTurnNumberGreaterThan`(8)
    - `PauseForAnimationToMap`(0)

### #30 Decolonization  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Early War
- text:

  > Add one USSR Influence in each of any four African and/or SE Asian countries.

- effect_spec:

  - **event_effects**:
    - `AddInfluenceInAfricaOrSEAsiaMax1`(4)

### #31 Red Scare/Purge  [IMPLEMENTED]

- side: **Neutral**, ops: **4**, stage: Early War
- text:

  > All further Operations cards played by your opponent this turn are -1 to their value (to a minimum of 1 Op).

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `DecreaseOperationsPointsForOpponentsCards`(1)
  - **event_effects**:
    - **_seq**:
      - `PutThisCardInPlay`(1)
    - **usageconditions**:
      - `NotIfChinaCardIsUnclaimedAndIsUSSR`(0)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #32 UN Intervention  [IMPLEMENTED]

- side: **Neutral**, ops: **1**, stage: Early War
- flags: cannot headline
- text:

  > Play this card simultaneously with a card containing your opponent's associated Event. The Event is cancelled, but you may use its Operations value to Conduct Operations. The cancelled event returns to the discard pile. May not be played during headline phase.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `ConductOperationsWithOpponentEventCard`(0)
    - **usageconditions**:
      - `IfIsNotHeadlinePhase`(0)
      - `IfHaveOpponentEventInHand`(0)

### #33 De-Stalinization  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Early War
- flags: remove-on-event (*)
- text:

  > USSR may relocate up to 4 Influence points to non-US controlled countries. No more than 2 Influence may be placed in the same country.

- effect_spec:

  - **event_effects**:
    - `RelocateInfluenceToUncontrolledCountriesMax2`(4)

### #34 Nuclear Test Ban  [IMPLEMENTED]

- side: **Neutral**, ops: **4**, stage: Early War
- text:

  > Player earns VPs equal to the current DEFCON level minus 2, then improve DEFCON two levels.

- effect_spec:

  - **event_effects**:
    - `GainVictoryPointsForDEFCONMinus`(2)
    - `ImproveDEFCONLevel`(2)

### #35 Formosan Resolution  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Early War
- flags: remove-on-event (*)
- text:

  > Taiwan shall be treated as a Battleground country for scoring purposes, if the US controls Taiwan when the Asia Scoring Card is played or during Final Scoring at the end of Turn 10. Taiwan is not a battleground country for any other game purpose. This card is discarded after US play of 'The China Card'.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `TaiwanIsBattlegroundForScoringIfUSControlled`(0)
  - **event_effects**:
    - **_seq**:
      - `PutThisCardInPlay`(0)
    - **usageconditions**:
      - `NotIfChinaCardIsUnclaimedAndIsUSA`(0)
  - **triggered_effects**:
    - **conditions**:
      - `WhenYouPlayChinaCard`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #103 Defectors  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Early War
- text:

  > Play in Headline Phase to cancel USSR Headline event, including Scoring Card. Cancelled card returns to the Discard Pile.
  > If Defectors played by USSR during Soviet action round, US gains 1 VP (unless played on the Space Race).

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `CancelPlayerHeadline`(0)
    - **condition**:
      - `IfIsHeadlinePhase`(0)
    - **_seq**:
      - `GainVictoryPoints`(1)
    - **condition**:
      - `IfIsOpponentActionRound`(0)

### #104 The Cambridge Five  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Early War
- flags: OPTIONAL
- text:

  > The US player exposes all scoring cards in their hand. The USSR player may then add 1 Influence in any single region named on one of those scoring cards (USSR choice). Cannot be played as an event in Late War.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `CommitPlayerDecision`(0)
      - `OpponentRevealsScoringCardsInHand`(0)
      - `AddInfluenceInOpponentScoringCardRegion`(1)
    - **usageconditions**:
      - `IfTurnNumberLessThan`(7)

### #105 Special Relationship  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Early War
- flags: OPTIONAL
- text:

  > If UK is US controlled but NATO is not in effect, US adds 1 Influence to any country adjacent to the UK.
  > If UK is US controlled and NATO is in effect, US adds 2 Influence to any Western European country and gains 2 VPs.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `AddInfluenceToOneWesternEuropeCountry`(2)
    - **condition**:
      - `IfControlUKAndNATO`(0)
    - **_seq**:
      - `GainVictoryPoints`(2)
    - **condition**:
      - `IfControlUKAndNATO`(0)
    - **_seq**:
      - `AddInfluenceAdjacentToUK`(1)
    - **condition**:
      - `IfControlUKButNoNATO`(8)

### #106 NORAD  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Early War
- flags: OPTIONAL, remove-on-event (*)
- text:

  > If the US controls Canada, the US may add 1 Influence to any country already containing US Influence at the conclusion of any Action Round in which the DEFCON marker moves to the '2' box. This event cancelled by 'Quagmire'.

- effect_spec:

  - **event_effects**:
    - `PutThisCardInPlay`(0)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfActionRound`(0)
      - `IfDefconDroppedTo2`(0)
      - `IfYouControlCanada`(0)
    - **triggereffect**:
      - `TriggerAnnounceCardInPlay`(0)
      - `TriggerAddInfluenceToExisting`(1)

## Mid War

### #36 Brush War  [IMPLEMENTED]

- side: **Neutral**, ops: **3**, stage: Mid War
- text:

  > Attack any country with a stability of 1 or 2. Roll a die and subtract 1 for every adjacent enemy controlled country. Success on 3-6. Player adds 3 to his Military Ops Track.
  > Effects of Victory: Player gains 1 VP and replaces all opponent's Influence with his Influence.

- effect_spec:

  - **event_effects**:
    - `BrushWarInStabilityLowerThan2`(769)   (packed: low=1, high=3)

### #37 Central America Scoring  [IMPLEMENTED]

- side: **Neutral**, ops: **0**, stage: Mid War
- flags: must be played, scoring: Central America
- text:

  > Both sides score:
  > Presence: 1
  > Domination: 3
  > Control: 5
  > +1 per controlled Battleground Country in Region
  > +1 per Country controlled that is adjacent to enemy superpower

- effect_spec:

  - **event_effects**:
    - `ScoreCentralAmerica`(0)

### #38 Southeast Asia Scoring  [IMPLEMENTED]

- side: **Neutral**, ops: **0**, stage: Mid War
- flags: remove-on-event (*), must be played, scoring: Southeast Asia
- text:

  > Both sides score:
  > 1 VP each for Control of: Burma, Cambodia/Laos,
  >     Vietnam, Malaysia, Indonesia, the Phillipines
  > 2 VP for Control of Thailand

- effect_spec:

  - **event_effects**:
    - `ScoreSoutheastAsia`(0)

### #39 Arms Race  [IMPLEMENTED]

- side: **Neutral**, ops: **3**, stage: Mid War
- text:

  > Compare each player's status on the Military Operations Track. If Phasing Player has more Military Operations points, he scores 1 VP. If Phasing Player has more Military Operations points and has met the Required Military Operations amount, he scores 3 VP instead.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `GainVictoryPoints`(1)
    - **condition**:
      - `IfHasMoreMilOpsAndNotRequired`(0)
    - **_seq**:
      - `GainVictoryPoints`(3)
    - **condition**:
      - `IfHasMoreMilOpsAndRequired`(0)

### #40 Cuban Missile Crisis  [IMPLEMENTED]

- side: **Neutral**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > Set DEFCON to Level 2. Any further Coup attempt by your opponent this turn, anywhere on the board, will result in Global Thermonuclear War. Your opponent will lose the game. This event may be cancelled at any time if the USSR player removes two Influence from Cuba or the US player removes 2 Influence from either West Germany or Turkey.

- effect_spec:

  - **cardinplay_abilities**:
    - **_seq**:
      - `RemoveInfluenceInCuba`(2)
      - `PauseForAnimationToMap`(0)
      - `RemoveThisCardFromPlay`(1)
    - **ability_name**:
    - **conditions**:
      - `IfPlayerIsUSSR`(0)
      - `IfPlayerHasInfluenceInCuba`(2)
    - **description**:
    - **_seq**:
      - `RemoveInfluenceInWestGermany`(2)
      - `PauseForAnimationToMap`(0)
      - `RemoveThisCardFromPlay`(1)
    - **ability_name**:
    - **conditions**:
      - `IfPlayerIsUS`(0)
      - `IfPlayerHasInfluenceInWestGermany`(2)
    - **description**:
    - **_seq**:
      - `RemoveInfluenceInTurkey`(2)
      - `PauseForAnimationToMap`(0)
      - `RemoveThisCardFromPlay`(1)
    - **ability_name**:
    - **conditions**:
      - `IfPlayerIsUS`(0)
      - `IfPlayerHasInfluenceInTurkey`(2)
    - **description**:
  - **event_effects**:
    - `SetDEFCONLevel`(2)
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #41 Nuclear Subs  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > US Coup attempts in Battleground Countries do not affect the DEFCON track for the remainder of the turn (does not affect Cuban Missile Crisis).

- effect_spec:

  - **event_effects**:
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #42 Quagmire  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > On next action round, US player must discard an Operations card worth 2 or more and roll 1-4 to cancel this event. Repeat each US player Action round until successful or no appropriate cards remain. If out of appropriate cards, the US player may only play scoring cards until the next turn.

- effect_spec:

  - **event_effects**:
    - `PutOpponentIntoQuagmire`(0)
    - `RemoveNORADFromPlay`(0)

### #43 Salt Negotiations  [IMPLEMENTED]

- side: **Neutral**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > Improve DEFCON two levels.
  > Further Coup attempts incur -1 die roll modifier for both players for the remainder of the turn.
  > Player may sort through discard pile and reclaim one non-scoring card, after revealing it to their opponent.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `ReduceAllCoupAttempts`(1)
  - **event_effects**:
    - `ImproveDEFCONLevel`(2)
    - `RecoverEventCardFromDiscardPile`(1)
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #44 Bear Trap  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > On next action round, USSR player must discard an Operations card worth 2 or more and roll 1-4 to cancel this event. Repeat each USSR player Action Round until successful or no appropriate cards remain. If out of appropriate cards, the USSR player may only play scoring cards until the next turn.

- effect_spec:

  - **event_effects**:
    - `PutOpponentIntoBearTrap`(0)

### #45 Summit  [IMPLEMENTED]

- side: **Neutral**, ops: **1**, stage: Mid War
- text:

  > Both players roll a die.  Each adds 1 for each Region they Dominate or Control. High roller gains 2 VP and may move DEFCON marker one level in either direction. Do not reroll ties.

- effect_spec:

  - **event_effects**:
    - `CommitPlayerDecision`(0)
    - `RestorePendingDefconLevel`(0)
    - `ResolveSummit`(2)

### #46 How I Learned to Stop Worrying  [IMPLEMENTED]

- side: **Neutral**, ops: **2**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > Set the DEFCON at any level you want (1-5). This event counts as 5 Military Operations for the purpose of required Military Operations.

- effect_spec:

  - **event_effects**:
    - `ChooseDEFCONLevel`(0)
    - `GainMilitaryOperationsTrack`(5)

### #47 Junta  [IMPLEMENTED]

- side: **Neutral**, ops: **2**, stage: Mid War
- text:

  > Place 2 Influence in any one Central or South American country.
  > Then you may make a free Coup attempt or Realignment roll in one of these regions (using this card's Operations Value).

- effect_spec:

  - **event_effects**:
    - `AddInfluenceToOneCentralOrSouthAmericaCountry`(2)
    - `MakeFreeCoupOrRealignmentsInCentralOrSouthAmerica`(2)

### #48 Kitchen Debates  [IMPLEMENTED]

- side: **USA**, ops: **1**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > If the US controls more Battleground countries than the USSR, poke opponent in chest and gain 2 VP!

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `GainVictoryPoints`(2)
    - **usageconditions**:
      - `IfYouControlMoreBattlegrounds`(0)

### #49 Missile Envy  [IMPLEMENTED]

- side: **Neutral**, ops: **2**, stage: Mid War
- text:

  > Exchange this card for your opponent's highest valued Operations card in his hand. If two or more cards are tied, opponent chooses.
  > If the exchanged card contains your event, or an event applicable to both players, it occurs immediately. If it contains opponent's event, use Operations value without triggering event. Opponent must use this card for Operations during his next action round.

- effect_spec:

  - **event_effects**:
    - `RemoveThisCardFromDiscardPile`(0)
    - `ResolveMissileEnvy`(0)
    - `DoNotDiscardThisCard`(0)

### #50 We Will Bury You  [IMPLEMENTED]

- side: **USSR**, ops: **4**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > Unless UN Invervention is played as an Event on the US player's next round, USSR gains 3 VP prior to any US VP award.
  > Degrade DEFCON one level.

- effect_spec:

  - **event_effects**:
    - `RestorePendingDefconLevel`(0)
    - `DegradeDEFCONLevel`(1)
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `AtStartOfOpponentActionRound`(0)
    - **triggereffect**:
      - `TriggerIncrementEffectData`(0)
    - **conditions**:
      - `WhenOpponentPlaysUNInterventionAsEvent`(0)
      - `IfEffectDataIsNotZero`(0)
      - `IfEffectDataIsZero`(1)
    - **triggereffect**:
      - `TriggerIncrementEffectData`(1)
    - **conditions**:
      - `WhenOpponentPlaysNotUNInterventionAsEvent`(0)
      - `IfEffectDataIsNotZero`(0)
      - `IfEffectDataIsZero`(1)
    - **triggereffect**:
      - `TriggerAnnounceCardInPlay`(0)
      - `TriggerGainVictoryPoints`(3)
      - `TriggerIncrementEffectData`(1)
    - **conditions**:
      - `AtEndOfActionRound`(0)
      - `IfEffectDataIsNotZero`(0)
      - `IfEffectDataIsZero`(1)
    - **triggereffect**:
      - `TriggerAnnounceCardInPlay`(0)
      - `TriggerGainVictoryPoints`(3)
      - `TriggerIncrementEffectData`(1)
    - **conditions**:
      - `AtEndOfActionRound`(0)
      - `IfEffectDataIsNotZero`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(0)

### #51 Brezhnev Doctrine  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > All further Operations cards played by the USSR this turn increase their Ops value by one (to a maximum of 4).

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `IncreaseOperationsPointsForYourCards`(1)
  - **event_effects**:
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #52 Portuguese Empire Crumbles  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > Add 2 USSR Influence in both SE African States and Angola.

- effect_spec:

  - **event_effects**:
    - `GainInfluenceInSEAfricanStates`(2)
    - `GainInfluenceInAngola`(2)
    - `PauseForAnimationToMap`(0)

### #53 South African Unrest  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Mid War
- text:

  > USSR either adds 2 Influence in South Africa or adds 1 Influence in South Africa and 2 Influence in any countries adjacent to South Africa.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - **_seq**:
        - `GainInfluenceInSouthAfrica`(2)
        - `PauseForAnimationToMap`(0)
      - **description**:
      - **_seq**:
        - `GainInfluenceInSouthAfrica`(1)
        - `AddInfluenceAdjacentToSouthAfrica`(2)
        - `PauseForAnimationToMap`(0)
      - **description**:
    - **prompt**:

### #54 Allende  [IMPLEMENTED]

- side: **USSR**, ops: **1**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > USSR receives 2 Influence in Chile.

- effect_spec:

  - **event_effects**:
    - `GainInfluenceInChile`(2)
    - `PauseForAnimationToMap`(0)

### #55 Willy Brandt  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > USSR receives gains 1 VP.
  > USSR receives 1 Influence in West Germany.
  > Cancels NATO for West Germany.
  > This event unplayable and/or cancelled by Tear Down This Wall.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `GainVictoryPoints`(1)
      - `GainInfluenceInWestGermany`(1)
      - `PutThisCardInPlay`(0)
    - **usageconditions**:
      - `IfHasNotBeenPlayedTearDownThisWall`(0)

### #56 Muslim Revolution  [IMPLEMENTED]

- side: **USSR**, ops: **4**, stage: Mid War
- text:

  > Remove all US Influence in two of the following countries: Sudan, Iran, Iraq, Egypt, Libya, Saudi Arabia, Syria, Jordan.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `RemoveAllOpponentInfluenceForMuslimRevolution`(2)
      - `PauseForAnimationToMap`(0)
    - **usageconditions**:
      - `IfHasNotBeenPlayedAWACSSaleToSaudis`(0)

### #57 ABM Treaty  [IMPLEMENTED]

- side: **Neutral**, ops: **4**, stage: Mid War
- text:

  > Improve DEFCON one level.
  > Then player may Conduct Operations as if they played a 4 Ops card.

- effect_spec:

  - **event_effects**:
    - `ImproveDEFCONLevel`(1)
    - `ConductOperationsWithThisCard`(4)

### #58 Cultural Revolution  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > If the US has 'The China Card', claim it face up and available for play. If the USSR already had it, USSR gains 1 VP.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - **_seq**:
        - `GainVictoryPoints`(1)
      - **condition**:
        - `IfYouHaveChinaCard`(0)
      - **_seq**:
        - `ClaimChinaCard`(0)
      - **condition**:
        - `IfYouDoNotHaveChinaCard`(0)
    - **usageconditions**:
      - `NotIfChinaCardIsUnclaimedAndIsUSSR`(0)

### #59 Flower Power  [IMPLEMENTED]

- side: **USSR**, ops: **4**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > USSR gains 2 VP for every subsequently US played 'war card' (played as an Event or Operations) unless played on the Space Race.
  > War Cards: Arab-Israeli War, Korean War, Brush War, Indo-Pakistani War or Iran-Iraq War.
  > This event cancelled by 'An Evil Empire'.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `PutThisCardInPlay`(0)
    - **usageconditions**:
      - `IfHasNotBeenPlayedAnEvilEmpire`(0)
  - **triggered_effects**:
    - **conditions**:
      - `WhenOpponentPlaysWarCard`(0)
    - **triggereffect**:
      - `TriggerAnnounceCardInPlay`(0)
      - `TriggerGainVictoryPoints`(2)

### #60 U2 Incident  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > USSR gains 1VP.
  > If UN Intervention played later this turn as an Event, either by US or USSR, gain 1 additional VP.

- effect_spec:

  - **event_effects**:
    - `GainVictoryPoints`(1)
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `WhenUNInventionIsPlayedAsEvent`(0)
    - **triggereffect**:
      - `TriggerAnnounceCardInPlay`(0)
      - `TriggerGainVictoryPoints`(1)
      - `TriggerRemoveThisCardFromPlay`(1)
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #61 OPEC  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Mid War
- text:

  > USSR gains 1VP for each of the following countries he controls:
  > Egypt, Iran, Libya, Saudi Arabia, Iraq, Gulf States, and Venezuela.
  > Unplayable as an event if 'North Sea Oil' is in effect.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `GainVPForOPEC`(1)
    - **usageconditions**:
      - `IfHasNotBeenPlayedNorthSeaOil`(0)

### #62 Lone Gunman  [IMPLEMENTED]

- side: **USSR**, ops: **1**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > US player reveals his hand. Then the USSR may Conduct Operations as if they played a 1 Op card.

- effect_spec:

  - **event_effects**:
    - `CommitPlayerDecision`(0)
    - `RestorePendingDefconLevel`(0)
    - `OpponentRevealsHand`(0)
    - `ConductOperationsWithThisCard`(1)

### #63 Colonial Rear Guards  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Mid War
- text:

  > Add 1 US Influence in each of four different African and/or Southeast Asian countries.

- effect_spec:

  - **event_effects**:
    - `AddInfluenceInAfricaOrSEAsiaMax1`(4)

### #64 Panama Canal Returned  [IMPLEMENTED]

- side: **USA**, ops: **1**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > Add 1 US Influence in Panama, Costa Rica, and Venezuela.

- effect_spec:

  - **event_effects**:
    - `GainInfluenceInPanama`(1)
    - `GainInfluenceInCostaRica`(1)
    - `GainInfluenceInVenezuela`(1)
    - `PauseForAnimationToMap`(0)

### #65 Camp David Accords  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > US gains 1 VP.
  > US receives 1 Influence in Israel, Jordan and Egypt.
  > Arab-Israeli War event no longer playable.

- effect_spec:

  - **event_effects**:
    - `GainVictoryPoints`(1)
    - `GainInfluenceInIsrael`(1)
    - `GainInfluenceInJordan`(1)
    - `GainInfluenceInEgypt`(1)
    - `PauseForAnimationToMap`(0)
    - `PutThisCardInPlay`(0)

### #66 Puppet Governments  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > US may add 1 Influence in three countries that currently contain no Influence from either power.

- effect_spec:

  - **event_effects**:
    - `AddInfluenceInEmptyCountriesMax1`(3)

### #67 Grain Sales to Soviets  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Mid War
- text:

  > Randomly choose one card from USSR hand. Play it or return it. If Soviet player has no cards, or returned, use this card to conduct Operations normally.

- effect_spec:

  - **event_effects**:
    - `CommitPlayerDecision`(0)
    - `RestorePendingDefconLevel`(0)
    - `MayPlayRandomCardFromOpponentHand`(0)
    - **_seq**:
      - `ConductOperationsWithThisCard`(2)
    - **condition**:
      - `IfYouDoNot`(2)

### #68 John Paul II Elected Pope  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > Remove 2 USSR Influence in Poland and then add 1 US Influence in Poland.
  > Allows play of 'Solidarity'.

- effect_spec:

  - **event_effects**:
    - `RemoveOpponentInfluenceInPoland`(2)
    - `GainInfluenceInPoland`(1)
    - `PauseForAnimationToMap`(0)
    - `PutThisCardInPlay`(0)

### #69 Latin American Death Squads  [IMPLEMENTED]

- side: **Neutral**, ops: **2**, stage: Mid War
- text:

  > All of the player's Coup attempts in Central and South America are +1 for the remainder of the turn, while all opponent's Coup attempts are -1 for the remainder of the turn.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `AdjustCoupAttemptsInCentralAndSouthAmerica`(1)
  - **event_effects**:
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #70 OAS Founded  [IMPLEMENTED]

- side: **USA**, ops: **1**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > Add 2 US Influence in Central America and/or South America.

- effect_spec:

  - **event_effects**:
    - `AddInfluenceInCentralAmericaOrSouthAmerica`(2)

### #71 Nixon Plays The China Card  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > If US has 'The China Card', gain 2 VP. Otherwise, US player receives 'The China Card' now, face down and unavailable for immediate play.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `GainVictoryPoints`(2)
    - **condition**:
      - `IfYouHaveChinaCard`(0)
    - **_seq**:
      - `ClaimChinaCard`(1)
    - **condition**:
      - `IfYouDoNotHaveChinaCard`(0)

### #72 Sadat Expels Soviets  [IMPLEMENTED]

- side: **USA**, ops: **1**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > Remove all USSR Influence in Egypt and add one US Influence.

- effect_spec:

  - **event_effects**:
    - `RemoveAllOpponentInfluenceInEgpyt`(0)
    - `GainInfluenceInEgypt`(1)
    - `PauseForAnimationToMap`(0)

### #73 Shuttle Diplomacy  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Mid War
- text:

  > Play in front of US player. During the next scoring of the Middle East or Asia (whichever comes first), subtract one Battleground country from USSR total, then put this card in the discard pile. Does not count for Final Scoring at the end of Turn 10.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `OpponentIgnoresBattlegroundInAsiaOrMiddleEast`(1)
  - **event_effects**:
    - `RemoveThisCardFromDiscardPile`(0)
    - `PutThisCardInPlaySpecial`(0)
    - `DoNotDiscardThisCard`(0)
  - **triggered_effects**:
    - **conditions**:
      - `WhenScoringAsiaOrMiddleEast`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlayAndDiscard`(1)

### #74 The Voice Of America  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Mid War
- text:

  > Remove 4 USSR Influence from non-European countries. No more than 2 may be removed from any one country.

- effect_spec:

  - **event_effects**:
    - `RemoveOpponentInfluenceFromNonEuropeMax2`(4)
    - `PauseForAnimationToMap`(0)

### #75 Liberation Theology  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Mid War
- text:

  > Add 3 USSR Influence in Central America, no more than 2 per country.

- effect_spec:

  - **event_effects**:
    - `AddInfluenceInCentralAmericaMax2`(3)

### #76 Ussuri River Skirmish  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > If the USSR has 'The China Card', claim it face up and available for play. If the US already has 'The China Card', add 4 US Influence in Asia, no more than 2 per country.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `AddInfluenceInAsiaMax2`(4)
    - **condition**:
      - `IfYouHaveChinaCard`(0)
    - **_seq**:
      - `ClaimChinaCard`(0)
    - **condition**:
      - `IfYouDoNotHaveChinaCard`(0)

### #77 Ask Not What Your Country Can Do For You...  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > US player may discard up to entire hand (including Scoring cards) and draw replacements from the deck. The number of cards discarded must be decided prior to drawing any replacements.

- effect_spec:

  - **event_effects**:
    - `MayDiscardEntireHand`(0)
    - `CommitPlayerDecision`(0)
    - `DrawCardForEachDiscard`(0)

### #78 Alliance for Progress  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Mid War
- flags: remove-on-event (*)
- text:

  > US gains 1 VP for each US controlled Battleground country in Central America and South America.

- effect_spec:

  - **event_effects**:
    - `GainVPForAllianceForProgress`(1)

### #79 Africa Scoring  [IMPLEMENTED]

- side: **Neutral**, ops: **0**, stage: Mid War
- flags: must be played, scoring: Africa
- text:

  > Both sides score:
  > Presence: 1
  > Domination: 4
  > Control: 6
  > +1 per controlled Battleground Country in Region

- effect_spec:

  - **event_effects**:
    - `ScoreAfrica`(0)

### #80 One Small Step...  [IMPLEMENTED]

- side: **Neutral**, ops: **2**, stage: Mid War
- text:

  > If you are behind on the Space Race Track, play this card to move your marker two boxes forward on the Space Rack Track, gaining the VP value of the second box only.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `AdvanceSpaceRaceTrack`(2)
    - **usageconditions**:
      - `IfYouAreBehindOnSpaceRaceTrack`(0)

### #81 South America Scoring  [IMPLEMENTED]

- side: **Neutral**, ops: **0**, stage: Mid War
- flags: must be played, scoring: South America
- text:

  > Both sides score:
  > Presence: 2
  > Domination: 5
  > Control: 6
  > +1 per controlled Battleground Country in Region

- effect_spec:

  - **event_effects**:
    - `ScoreSouthAmerica`(0)

### #107 Che  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Mid War
- flags: OPTIONAL
- text:

  > USSR may immediately make a Coup attempt using this card's Operations value against a non-battleground country in Central America, South America, or Africa. If the Coup removes any US Influence, USSR may make a second Coup attempt against a different target under the same restrictions.

- effect_spec:

  - **event_effects**:
    - `MakeCoupAttemptInNonBattlegroundInAmericasOrAfrica`(3)
    - **_seq**:
      - `MakeCoupAttemptInNonBattlegroundInAmericasOrAfrica`(3)
    - **condition**:
      - `IfYouDo`(0)

### #108 Our Man In Tehran  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Mid War
- flags: OPTIONAL, remove-on-event (*)
- text:

  > If the US controls at least one Middle East country, the US player draws the top 5 cards from the draw pile. They may reveal and then discard any or all of these drawn cards without triggering the Event. Any remaining drawn cards are returned to the draw deck, and it is reshuffled.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `CommitPlayerDecision`(0)
      - `MayDiscardTopOfDrawPile`(5)
    - **usageconditions**:
      - `IfYouControlMiddleEastCountry`(1)

## Late War

### #82 Iranian Hostage Crisis  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Late War
- flags: remove-on-event (*)
- text:

  > Remove all US Influence in Iran. Add 2 USSR Influence in Iran.
  > Doubles the effect of Terrorism card against US.

- effect_spec:

  - **event_effects**:
    - `RemoveAllOpponentInfluenceInIran`(0)
    - `GainInfluenceInIran`(2)
    - `PauseForAnimationToMap`(0)
    - `PutThisCardInPlay`(0)

### #83 The Iron Lady  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Late War
- flags: remove-on-event (*)
- text:

  > US gains 1 VP.
  > Add 1 USSR Influence in Argentina. Remove all USSR Influence from UK.
  > Socialist Governments event no longer playable.

- effect_spec:

  - **event_effects**:
    - `GainVictoryPoints`(1)
    - `OpponentGainsInfluenceInArgentina`(1)
    - `RemoveAllOpponentInfluenceInUK`(0)
    - `PauseForAnimationToMap`(0)
    - `PutThisCardInPlay`(0)

### #84 Reagan Bombs Libya  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Late War
- flags: remove-on-event (*)
- text:

  > US gains 1 VP for every 2 USSR Influence in Libya.

- effect_spec:

  - **event_effects**:
    - `GainVPForOpponentInfluenceInLibya`(2)

### #85 Star Wars  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Late War
- flags: remove-on-event (*)
- text:

  > If the US is ahead on the Space Race Track, play this card to search through the discard pile for a non-scoring card of your choice. Event occurs immediately.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `CopyEventCardFromDiscardPile`(0)
    - **usageconditions**:
      - `IfUSIsAheadOnSpaceRaceTrack`(0)

### #86 North Sea Oil  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Late War
- flags: remove-on-event (*)
- text:

  > OPEC event is no longer playable.
  > US may play 8 cards this turn.

- effect_spec:

  - **event_effects**:
    - `SetActionRoundCount`(8)
    - `PutThisCardInPlay`(0)

### #87 The Reformer  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Late War
- flags: remove-on-event (*)
- text:

  > Add 4 Influence in Europe (no more than 2 per country). If USSR is ahead of US in VP, then 6 Influence may be added instead.
  > USSR may no longer conduct Coup attempts in Europe.
  > Improves effect of Glasnost event.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `YouCannotCoupInEurope`(0)
  - **event_effects**:
    - **_seq**:
      - `AddInfluenceInEuropeMax2`(6)
    - **condition**:
      - `IfYouAreAheadOnVPTrack`(0)
    - **_seq**:
      - `AddInfluenceInEuropeMax2`(4)
    - **condition**:
      - `IfYouAreNotAheadOnVPTrack`(0)
    - **_seq**:
      - `PutThisCardInPlay`(0)
    - **condition**:
      - `IfGlasnostIsNotInRemovedPile`(0)

### #88 Marine Barracks Bombing  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Late War
- flags: remove-on-event (*)
- text:

  > Remove all US Influence in Lebanon plus remove 2 additional US Influence from anywhere in the Middle East.

- effect_spec:

  - **event_effects**:
    - `RemoveAllOpponentInfluenceInLebanon`(0)
    - `PauseForAnimationToMap`(0)
    - `RemoveOpponentInfluenceInMiddleEast`(2)
    - `PauseForAnimationToMap`(0)

### #89 Soviets Shoot Down KAL-007  [IMPLEMENTED]

- side: **USA**, ops: **4**, stage: Late War
- flags: remove-on-event (*)
- text:

  > Degrade DEFCON one level. US gains 2 VP.
  > If South Korea is US Controlled, then the US may place Influence or attempt Realignment as if they played a 4 Ops card.

- effect_spec:

  - **event_effects**:
    - `RestorePendingDefconLevel`(0)
    - `DegradeDEFCONLevel`(1)
    - `GainVictoryPoints`(2)
    - **_seq**:
      - `PlaceInfluenceOrAttemptRealignmentsWithThisCard`(4)
    - **condition**:
      - `IfYouControlSouthKorea`(0)

### #90 Glasnost  [IMPLEMENTED]

- side: **USSR**, ops: **4**, stage: Late War
- flags: remove-on-event (*)
- text:

  > USSR gains 2 VP.
  > Improve DEFCON one level.
  > If The Reformer is in effect, then the USSR may place Influence or attempt Realignments as if they played a 4 Ops card.

- effect_spec:

  - **event_effects**:
    - `GainVictoryPoints`(2)
    - `ImproveDEFCONLevel`(1)
    - **_seq**:
      - `PlaceInfluenceOrAttemptRealignmentsWithThisCard`(4)
    - **condition**:
      - `IfHasBeenPlayedTheReformer`(0)
    - `RemoveTheReformerFromPlay`(0)

### #91 Ortega Elected in Nicaragua  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Late War
- flags: remove-on-event (*)
- text:

  > Remove all US Influence from Nicaragua. Then USSR may make one free Coup attempt (with this card's Operations value) in a country adjacent to Nicaragua.

- effect_spec:

  - **event_effects**:
    - `RemoveAllOpponentInfluenceInNicaragua`(0)
    - `PauseForAnimationToMap`(0)
    - `MakeFreeCoupAttemptAdjacentToNicaragua`(0)
    - `RestorePendingDefconLevel`(0)

### #92 Terrorism  [IMPLEMENTED]

- side: **Neutral**, ops: **2**, stage: Late War
- text:

  > Opponent must randomly discard one card. If played by USSR and Iranian Hostage Crisis is in effect, the US player must randomly discard two cards.
  > (Events on discards do not occur.)

- effect_spec:

  - **event_effects**:
    - `CommitPlayerDecision`(0)
    - `OpponentDiscardsRandomCard`(0)
    - **_seq**:
      - `OpponentDiscardsRandomCard`(0)
    - **condition**:
      - `IfUSSRAndHasBeenPlayedIranianHostageCrisis`(0)

### #93 Iran-Contra Scandal  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Late War
- flags: remove-on-event (*)
- text:

  > All US Realignment rolls have a -1 die roll modifier for the remainder of the turn.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `OpponentRealignmentRollsAreReducedBy`(1)
  - **event_effects**:
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #94 Chernobyl  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Late War
- flags: remove-on-event (*)
- text:

  > The US player may designate one Region. For the remainder of the turn the USSR may not add additional Influence to that Region by the play of Operations Points via placing Influence.

- effect_spec:

  - **continuous_effects**:
    - **effect**:
      - `OpponentMayNotPlaceInfluenceInSelectedRegion`(0)
  - **event_effects**:
    - `ChooseOneRegion`(0)
    - `PutThisCardInPlayWithRegionChoice`(0)
  - **triggered_effects**:
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #95 Latin American Debt Crisis  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Late War
- text:

  > Unless the US Player immediately discards a '3' or greater Operations card, double USSR Influence in two countries in South America.

- effect_spec:

  - **event_effects**:
    - `OpponentMayDiscardCardWithOpsPoints`(3)
    - **_seq**:
      - `DoubleInfluenceInSouthAmericanCountries`(2)
    - **condition**:
      - `IfYouDoNot`(0)

### #96 Tear Down This Wall  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Late War
- flags: remove-on-event (*)
- text:

  > Cancels/prevent Willy Brandt.
  > Add 3 US Influence in East Germany.
  > Then US may make a free Coup attempt or Realignment rolls in Europe using this card's Ops Value.

- effect_spec:

  - **event_effects**:
    - `RemoveWillyBrandtFromPlay`(0)
    - `GainInfluenceInEastGermany`(3)
    - `PauseForAnimationToMap`(0)
    - `MakeFreeCoupOrRealignmentAttemptsInEurope`(0)
    - `RestorePendingDefconLevel`(0)
    - **_seq**:
      - `PutThisCardInPlay`(0)
    - **condition**:
      - `IfWillyBrandtIsNotInRemovedPile`(0)

### #97 An Evil Empire  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Late War
- flags: remove-on-event (*)
- text:

  > Cancels/Prevents Flower Power.
  > US gains 1 VP.

- effect_spec:

  - **event_effects**:
    - `RemoveFlowerPowerFromPlay`(0)
    - `GainVictoryPoints`(1)
    - **_seq**:
      - `PutThisCardInPlay`(0)
    - **condition**:
      - `IfFlowerPowerIsNotInRemovedPile`(0)

### #98 Aldrich Ames Remix  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Late War
- flags: remove-on-event (*)
- text:

  > US player exposes his hand to USSR player for remainder of turn. USSR then chooses one card from US hand; this card is discarded.

- effect_spec:

  - **event_effects**:
    - `CommitPlayerDecision`(0)
    - `DiscardCardFromOpponentHand`(0)

### #99 Pershing II Deployed  [IMPLEMENTED]

- side: **USSR**, ops: **3**, stage: Late War
- flags: remove-on-event (*)
- text:

  > USSR gains 1 VP.
  > Remove 1 US Influence from up to three countries in Western Europe.

- effect_spec:

  - **event_effects**:
    - `GainVictoryPoints`(1)
    - `RemoveOpponentInfluenceInWesternEuropeMax1`(3)
    - `PauseForAnimationToMap`(0)

### #100 Wargames  [IMPLEMENTED]

- side: **Neutral**, ops: **4**, stage: Late War
- flags: remove-on-event (*)
- text:

  > If DEFCON Status 2, you may immediately end the game (without Final Scoring Phase) after giving opponent 6 VPs.
  > How about a nice game of chess?

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `ResolveWargames`(6)
    - **usageconditions**:
      - `IfDefconLevelIs`(2)

### #101 Solidarity  [IMPLEMENTED]

- side: **USA**, ops: **2**, stage: Late War
- flags: remove-on-event (*)
- text:

  > Playable as an event only if John Paul II Elected Pope is in effect.
  > Add 3 US Influence in Poland.

- effect_spec:

  - **event_effects**:
    - **_seq**:
      - `GainInfluenceInPoland`(3)
      - `PauseForAnimationToMap`(0)
      - `RemoveJohnPaulIIElectedPopeFromPlay`(0)
    - **usageconditions**:
      - `IfHasBeenPlayedJohnPaulIIElectedPope`(0)

### #102 Iran-Iraq War  [IMPLEMENTED]

- side: **Neutral**, ops: **2**, stage: Late War
- flags: remove-on-event (*)
- text:

  > Iran or Iraq invades the other (player's choice). Roll one die and subtract 1 for every opponent-controlled country adjacent to target of invasion. Player Victory on modified die roll of 4-6. Player adds 2 to Military Ops Track
  > Effects of Victory: Player gains 2 VP and replaces opponent's Influence in target country with his own.

- effect_spec:

  - **event_effects**:
    - `WarInIranOrIraq`(514)   (packed: low=2, high=2)

### #109 Yuri and Samantha  [IMPLEMENTED]

- side: **USSR**, ops: **2**, stage: Late War
- flags: OPTIONAL, remove-on-event (*)
- text:

  > USSR receives 1 VP for each US coup attempt made for the remainder of the current turn.

- effect_spec:

  - **event_effects**:
    - `PutThisCardInPlay`(1)
  - **triggered_effects**:
    - **conditions**:
      - `WhenOpponentMakesCoupAttempt`(0)
    - **triggereffect**:
      - `TriggerAnnounceCardInPlay`(0)
      - `TriggerGainVictoryPoints`(1)
    - **conditions**:
      - `AtEndOfTurn`(0)
    - **triggereffect**:
      - `TriggerRemoveThisCardFromPlay`(1)

### #110 AWACS Sale to Saudis  [IMPLEMENTED]

- side: **USA**, ops: **3**, stage: Late War
- flags: OPTIONAL, remove-on-event (*)
- text:

  > US receives 2 Influence in Saudi Arabia. Muslim Revolution may no longer be played as an event.

- effect_spec:

  - **event_effects**:
    - `GainInfluenceInSaudiArabia`(2)
    - `PauseForAnimationToMap`(0)
    - `PutThisCardInPlay`(0)

